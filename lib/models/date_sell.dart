class DateSell{
  final String? day;
  final String? month;
  final DateTime? dateTime;
  final int? id;
  bool isSelected = false;
  DateSell( {this.day, this.month, this.dateTime, this.id, required this.isSelected});
}